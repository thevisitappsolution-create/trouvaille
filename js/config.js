/* ============================================================
   TROUVAILLE — Configuration du PARTAGE EN LIGNE des mots
   ------------------------------------------------------------
   Les mots ajoutés par le créateur sont stockés dans le fichier
   `mots-partages.json` du dépôt GitHub. TOUS les joueurs le lisent
   (aucune clé nécessaire pour lire). Seul le créateur peut publier,
   grâce à un jeton GitHub gardé UNIQUEMENT sur son appareil
   (Réglages → mode créateur). Voir STORES.md pour le détail.

   -> Mets ici ton dépôt "owner/repo". C'est une info publique.
   ============================================================ */
const CONFIG = {
  partageRepo: "thevisitappsolution-create/trouvaille",
  partageFichier: "mots-partages.json",
  partageBranche: "main"
};
